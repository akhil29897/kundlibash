{
  "name": "kundlibash",
  "version": "1.0.0",
  "private": true,
  "description": "Bash Your Horoscope (Kundlibash) — predict it, bash it against reality, collect accuracy notes + community stats. Cloudflare Pages + Functions + KV.",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date 2024-09-23 --port 8788",
    "deploy": "wrangler pages deploy public --project-name kundlibash --branch main",
    "kv:create": "wrangler kv namespace create BASH_KV"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
